# 영상 첫/마지막 프레임 추출기

영상 파일을 선택하고 버튼 한 번을 누르면 **첫 프레임**과 **마지막 프레임**을 원본 해상도의 **무손실 PNG** 두 장으로 저장하는 로컬 데스크톱 프로그램입니다.

- 영상은 외부로 업로드되지 않으며, 내 컴퓨터에서 FFmpeg로만 처리됩니다.
- PNG는 무손실 포맷이므로 JPEG 재압축으로 인한 화질 저하가 없습니다.
- 큰 영상에서도 시작과 끝 구간을 중심으로 처리하도록 설계되었습니다.

## 설치

Python 3.9 이상을 설치한 뒤, 프로젝트 폴더에서 다음 명령을 실행하세요.

```bash
python -m pip install -r requirements.txt
```

`imageio-ffmpeg`가 FFmpeg 실행 파일을 자동으로 제공합니다. FFmpeg를 찾을 수 없다는 오류가 계속되면 시스템에 FFmpeg를 설치한 뒤 다시 실행하세요.

Linux에서는 Tkinter가 별도로 필요할 수 있습니다.

```bash
sudo apt install python3-tk
```

## 실행

```bash
python frame_extractor.py
```

Windows에서 `python` 명령이 작동하지 않으면 다음을 사용하세요.

```bash
py frame_extractor.py
```

## 사용 방법

1. 점선 영역에 영상을 끌어다 놓거나 클릭해 파일을 선택합니다.
2. 미리보기와 파일명, 해상도, FPS, 길이, 총 프레임 수를 확인합니다.
3. 필요하면 **변경** 버튼으로 저장 폴더를 지정합니다. 지정하지 않으면 원본 영상과 같은 폴더에 저장됩니다.
4. **프레임 추출 (첫 장면 + 마지막 장면)** 버튼을 누릅니다.

총 480프레임인 `영상이름.mp4`의 출력 예시는 아래와 같습니다.

```text
영상이름_first_frame1_of_480.png
영상이름_last_frame480_of_480.png
```

## 지원 형식

MP4, MOV, MKV, AVI, WebM, M4V, MTS/M2TS, MXF, WMV, FLV, TS, 3GP 등 FFmpeg가 읽을 수 있는 대부분의 영상 형식을 지원합니다.

## 색상 또는 대비가 다르게 보일 때

영상은 보통 YUV 방식으로 저장되며 PNG(RGB)로 변환하려면 색상 범위(Full/Limited)와 색공간(BT.709/BT.601) 정보가 필요합니다. 프로그램은 영상의 메타데이터를 우선 사용하고, 정보가 없으면 Limited/BT.709을 기본값으로 적용합니다.

미리보기가 원본과 다르면 화면의 **색상 범위**와 **색공간**을 바꿔 원본과 일치하는 조합을 찾으세요. 추출 시 현재 선택한 값이 적용됩니다.

## 문제 해결

- **ffmpeg를 찾을 수 없습니다**: `python -m pip install imageio-ffmpeg`를 다시 실행해 보세요. 오프라인 환경이라면 FFmpeg를 직접 설치하고 시스템 PATH에 추가할 수 있습니다.
- **드래그 앤 드롭이 작동하지 않습니다**: `python -m pip install tkinterdnd2`를 실행한 뒤 프로그램을 다시 시작하세요. 설치하지 않아도 클릭으로 영상 선택은 가능합니다.
- **마지막 프레임이 검은 화면입니다**: 일부 편집 영상은 끝에 빈 프레임을 포함합니다. 이 경우 실제로 원하는 마지막 유효 프레임의 기준을 확인한 뒤 추가 처리가 필요할 수 있습니다.

